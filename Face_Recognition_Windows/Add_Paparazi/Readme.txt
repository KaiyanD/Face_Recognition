Run mustache-cv2.py is to add mustache to your nose using cv2 to capture video. Require ffmpeg to be installed before OpenCV(so that Opencv could support video capture in raspberry pi)
Run mustache-pi is to add mustache to your nose using Picamera to capture video. Requires Picamera installed.

